export default function Button(props) {     // conceito de propriedad

  <>
  return <button>{props.children}</button>    // coloco chaves , indica que estou colocando code js, uma var;
  <span>{counter}</span>                              //conceito de estado
  </>
}
